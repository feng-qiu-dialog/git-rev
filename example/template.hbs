{
    "revision": "{{ revision }}",
    "rev_short": "{{ rev_short }}",
    "branch": "{{ branch }}",
    "commit_date": "{{ git_log_format "%aI" }}",
    "tag": "{{ tags.0 }}",
    "tags": [
        {{#each tags}}
        "{{this}}"
        {{/each}}
    ]
}